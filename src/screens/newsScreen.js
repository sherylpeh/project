import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const NewsScreen = () => {
  return <Text style={{ fontSize: 48 }}>NewsScreen</Text>;
};

const styles = StyleSheet.create({});

export default NewsScreen;